baseURL: https://www.costas.dev/
languageCode: es-ES
defaultContentLanguage: es
title: Ariel Costas

outputs:
  home: ["HTML"]
  posts: ["HTML", "RSS", "markdown"]

markup:
  goldmark:
    renderer:
      unsafe: true